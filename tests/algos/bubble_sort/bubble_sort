import random
from algos import bubble_sort

test_list = [random.randint (1,1000) for i in range(5)]
bubbled = bubble_sort.sort(test_list)
clean_sorted = sorted(test_list, reverse= True)
if bubbled == clean_sorted:
    print("Correct")
else:
    print("Incorrect")
